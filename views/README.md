# Views

This project is an API-only Express application, so it does not render server-side HTML views.

The `views` folder is included to satisfy the MVC structure requirement:

- `models/` contains database schemas
- `controllers/` contains request handlers
- `views/` is reserved for presentation templates if the project later adds server-rendered pages
